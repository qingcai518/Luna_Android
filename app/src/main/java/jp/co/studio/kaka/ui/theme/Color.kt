package jp.co.studio.kaka.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * 各主题的配色在 Palette.kt（和 iOS 的 ThemeManager.swift 一一对应）。这里只放不随主题变的颜色。
 */

// 首页主角卡强调色（都是亮色）上的按钮图标色，和 iOS HeroCell.onAccent 一致
val HeroInk = Color(0xFF0F0C2A)

// 破坏性操作
val LunaRed = Color(0xFFD15E5E)
